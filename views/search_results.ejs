<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Search Results for "<%= query %>"</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">
  <link rel="stylesheet" href="/public/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
  <%- include('partials/header') %>
  <div class="container-fluid">
    <div class="row">
      <aside class="col-md-3 col-lg-2 d-md-block bg-dark sidebar collapse">
        <div class="position-sticky pt-3">
          <h3 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
            <span>Favorite Accounts</span>
          </h3></h3>
        <ul class="nav flex-column">
          <% favoriteAccounts.forEach(account => { %>
            <li class="nav-item"><a class="nav-link" href="/profile/<%= account %>">@<%= account %></a></li>
          <% }); %>
        </ul>
        <h3 class="mt-3">All Accounts</h3>
        <ul class="nav flex-column">
          <% allAccounts.forEach(account => { %>
            <li class="nav-item"><a class="nav-link" href="/profile/<%= account %>">@<%= account %></a></li>
          <% }); %>
        </ul>
      </aside>
      <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 main-content">
        <header class="p-3 border-bottom">
          <h1>Search Results for "<%= query %>"</h1>
        </header>
          <% if (tweets && tweets.length > 0) { %>
            <% tweets.forEach(tweetData => { %>
              <%- include('_tweet', { tweetData: tweetData, buttonPartial: '_search_tweet_buttons' }) %>
            <% }); %>
          <% } else { %>
            <p class="text-center">No tweets found matching your search.</p>
          <% } %>
        </div>
      </main>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js" integrity="sha384-ndDqU0Gzau9qJ1lfW4pNLlhNTkCfHzAVBReH9diLvGRem5+R9g2FzA8ZGN954O5Q" crossorigin="anonymous"></script>
</body>
</html>
